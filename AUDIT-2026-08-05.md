# 链路闭环评审 2026-08-05

分支 `claude/code-review-full-path-mpqpos`，基线 `9a41828`（main HEAD）。

## 范围与方法

评审对象是最新合入的 PR #57（`f21b6bf..9a41828`，durable-routed topic knowledge，
3 个提交，+1169/−22），验收标准是**从入口到整条链路是否闭环**——每一跳都找到
真实的调用点，而不是相信提交说明。方法：

1. 逐行走读全部非测试 diff（`knowledge.py`、`vector.py`、`embeddings.py`、
   `store.py`、`search.py`、`history.py`、`compile.py`、`maintenance.py`、
   `preparation.py`、server schema、seed config）。
2. 沿五条链路逐跳验证衔接点（见下），每个衔接点核对两端的文件:行号。
3. 三端测试套件全量复跑；对唯一非文档化的失败项做根因定位并手工复现。

测试基线（本容器，Linux）：

| 套件 | 结果 | 与文档基线的差异 |
|---|---|---|
| engine | 1029 passed / **1 failed** / 59 skipped | 失败项见 P1-1，与 PR #57 无关 |
| server | 231 passed / 5 skipped | 全绿 |
| shell | 275 passed / 11 failed | 11 项 = CLAUDE.md §9 已记录的容器 auth_token 噪音 |

---

## 一、五条链路的闭环验证结果

### 1. 检索链（读路径）——闭合 ✓

shell (`index.tsx` → `api.ts`) → server (`session_service`) →
`preparation.py:387-390`（`services.embedding_provider` 覆盖 → `profile_config`
兜底）→ `store.retrieve_relevant_memory` → durable 词法选择 →
`TopicAssociationStore` 路由 → FTS 作用域搜索 + 可选向量检索 →
`assembler.py:515` episode 层注入。

- **哈希路由一致性**：写端 `knowledge._durable_entries`（sanitize → 按 `-` 取行
  → strip → sha256）与读端 `store.py:87-101`（`_select_relevant_durable` 同一
  `sanitize_memory_text` 源 → 同 strip → 同 `entry_id`）完全对齐，路由不会因
  规范化差异而静默失联。
- **旧 episode 不搁浅**：`store.py:122-126` 用 `indexed_ids - generated_ids`
  把快照之外的既有 episode 保留在无作用域召回里；有测试钉住
  （`test_snapshot_does_not_strand_episodes`）。
- **降级路径**：向量层任何异常回退 FTS（`store.py:143-144`）；路由层任何异常
  回退全量召回（`store.py:105-109`）；均不阻塞 prompt 组装。

### 2. 写入/生成链——闭合 ✓

turn 结束 → `lifecycle.py` → `MemoryLifecycleHooks.record_turn` →
`save_conversation_memory` 的 compile 回调。三个回调变体（内联
`_run_compilation_unlocked`、deferred `_schedule_compilation` 起的后台任务、
idle tick）**全部汇于** `maintenance.py:187` 的
`run_compilation(..., sync_topics=True)` → `sync_durable_topics` →
`compact_episode`（走既有生成+评审通道）→ `replace_all_topics` 快照替换 →
`_remove_topic_file` 清理被移除主题的页面与 `.bak`。没有旁路能在更新 durable
后跳过 topic 同步。

### 3. 失败重试链——闭合 ✓

`sync_durable_topics` 失败 → `compile.py:975-990` `mark_sync_pending()` +
`topic_sync` 冷却标记（不让派生视图拖垮已评审的 durable 更新，也不烧穿 LLM）→
server `scheduler.py:29` 的 idle tick → `run_idle_maintenance`
（`maintenance.py:158-171`：`is_sync_pending() and not _in_retry_cooldown`）→
重试。成功后 `_clear_retry_attempt` + `replace_all_topics` 清 pending。
空分类结果按失败处理而不是清空快照（`knowledge.py:203-208`，有测试）。

### 4. 状态观测链——server 为止闭合，shell 断开 ✗（见 P1-2）

`append_memory_history`（写侧已消毒 + 500 字符截断，`history.py:34-37`）→
`recent_failure_streak`（只读 64KB 尾部）→ `maintenance_status` →
`agent_service.py:143` → `GET /api/agent/memory/status`（server 路由测试已覆盖
新字段）→ **shell 未消费**。

### 5. 配置链——闭合（默认 fail-closed）✓

`agents/smith/config.yaml`（seed，注释示例）→ `profile_files.py:25` copy-once →
`~/.agent-smith/config.yaml` → `_load_profile_config` →
`embedding_provider_from_config`（要求显式 `enabled: true` + 三字段齐全 +
env 里真有 key，缺一即 None）。默认关闭，嵌入层完全可选。

---

## 二、发现

### P1-1 git_ops commit 丢弃用户 git 身份 → 无 repo-local 身份的仓库里提交必败

`agents/tools/git_ops.py:107-130`（`_safe_environment`，隔离始于 `eee2677`，
测试补于 `227bee8`；**非 PR #57 引入**，但它是最新 main 上唯一的引擎测试失败）

`_safe_environment` 把 `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` 指向 `/dev/null`
且 `HOME` 指向仓库目录。后果：经 Smith 的 git_ops 跑的每一条 git 命令都看不到
用户的全局身份。在没有 repo-local `user.email` 的仓库里：

- 宿主 FQDN 非法（本容器为 `vm.(none)`）→ `git commit` 以 exit 128 失败：
  `fatal: unable to auto-detect email address`。已手工复现。
- 宿主 FQDN 恰好合法 → git **杜撰** `user@fqdn` 身份写进提交历史，比失败更糟。

`test_ordinary_commit_still_works` 因此是机器相关的：在作者机器上通过、在本类
容器上必败——它断言了"普通提交能成功"，而当前实现只在碰巧能自动探测身份的
机器上成立。

**修法**：在工具侧（隔离环境之外）读取宿主的 `git config user.name/user.email`，
经 `GIT_AUTHOR_*`/`GIT_COMMITTER_*` 环境变量或 `-c` 参数显式注入 commit 调用；
或者只屏蔽危险配置键（`credential.helper`、`core.hooksPath`、pager/editor 等）
而不是清空整个全局配置。测试则应同时钉住两个契约：普通提交成功 + 身份来自
用户配置而非自动探测。

### P1-2 失败流水没有到达用户：shell 未消费 `consecutive_failures`/`last_error`

`shell/src/api.ts:130-134`、`shell/src/hud.tsx:242-249`

`d3cd2a7` 的提交说明是 "surface failure streaks"，schema 注释写明目的是
「让停摆的流水线（过期 key 每次 401）可见，而不是静默饿死记忆」。实际只通到
HTTP API 为止：shell 的 `MemoryMaintenance` 类型仍只有 `compile/nudge/dream`
三个字段，HUD 的 `memoryMaintenanceLabel` 也只渲染这三个状态。用户唯一的
客户端（Ink shell）看不到任何失败迹象——这正是本次评审要抓的"最后一跳断链"。

**修法**：`api.ts` 类型补上两个字段；`hud.tsx` 在 `consecutive_failures >= 3`
（阈值可调）时渲染如 `memory stalled ×N` 的标签，`last_error` 进 tooltip 或
detail 行。字段已在 server 侧消毒并截断，shell 侧按惯例再过一次
`sanitizeTerminalText` 即可。

### P2-1 生成的 topic 页与用户 episode 共享命名空间，级联删除可致用户记忆永久丢失

`engine/memory/compile.py:858-876`（同名 header 原地覆盖，仅留 `.bak`）+
`engine/memory/knowledge.py:281-293`（`_remove_topic_file` 把 `.md` 与
`.md.bak` 一并删除）

`memory_ops`（用户显式"总结这个话题"，`builtin_tools.py:16-25`）与
`sync_durable_topics` 都经 `compact_episode` 写 `episodes/{slug}.md`，且生成页
没有任何 provenance 标记。链条：

1. 用户 episode `部署.md`（header `# 部署`）已存在；
2. 分类器产出同名 topic「部署」→ `compact_episode` 判定"同 topic"，原地覆盖，
   用户内容只剩 `部署.md.bak`（不入索引，检索不可达）；association 认领该
   file id，此后它只响应 durable 路由；
3. 之后某次同步分类器不再产出「部署」→ `replace_all_topics` 报告 removed →
   `_remove_topic_file` 删除 `部署.md` **和** `部署.md.bak` —— 用户手写的
   episode 永久消失。

`.bak` 级联删除本意是别给"确属生成的页面"留孤儿备份（`d3cd2a7` 修的孤儿
问题），但它假设了"这个 slug 永远只属于生成页"，而第 2 步恰恰能把用户文件
卷进这个 slug。

**修法**（任选其一，代价递增）：
- 生成页加独立前缀或子目录（如 `episodes/topics/`），与用户 episode 物理隔离；
- 或 association 里记录生成页内容 hash，`_remove_topic_file` 删除前比对，
  hash 不符（说明被用户内容覆盖过）就只解除关联、不删文件；
- 最少也应让删除只删 `.md` 而保留 `.bak`。

### P3-1 向量缓存按"本次路由到的主题"整体重建 → 跨查询抖动重嵌 + 热路径写放大

`engine/memory/vector.py:32-63`（`sync` 用 `current` 全量替换 `items`）+
`store.py:131-142`（每次检索都调 `sync`）

`sync` 把 `items` 重建为**本次查询**路由到的主题的 chunk 集合：查询 A 路由
主题 X（嵌入 X 并写盘）→ 查询 B 路由主题 Y（丢弃 X 的向量）→ 再查 X 时全部
重嵌。每次命中路由的查询还无条件 `atomic_write_text` 一次 `vectors.json`，
即使内容毫无变化。页面 ≤800 字符、≤8 主题，绝对成本不大，但这是每轮对话的
热路径，且嵌入是计费 API。

**修法**：merge 而非 replace——保留不在本次 `documents` 里的既有 items（其
清理时机是主题从快照移除时，`removed` 已经有了确切的信号）；`items` 与既有
状态相等时跳过写盘。

### P3-2 topic_sync 没有重试上限，且失败不写 memory_history —— 状态探针的盲区

`compile.py:975-990`、`knowledge.py:239-241`

一条会让 `compact_episode` 持续失败的 durable bullet（topic 名触发
secret/injection 检测返回 None、或生成持续超字符预算抛
`MemoryCompilationError`）会让 sync 永远 pending：每过一次 600s 冷却就烧一次
分类器调用，无上限、无升级。同时这条失败路径只 `logger.warning`，**不写**
`memory_history.jsonl`——意味着 P1-2 修好之后，用户看到的 `consecutive_failures`
仍然数不到 topic_sync 的失败；`sync_pending` 本身也不在
`maintenance_status` 的输出里。派生视图的失败对唯一的观测通道完全不可见。

**修法**：失败路径补一条 `append_memory_history(target="topic_sync",
status="failed", ...)`（写侧已有消毒/截断），并在 `maintenance_status` 里带出
`topic_sync: pending|idle`；可选加连续失败计数上限后转为放弃+一次性告警。

### P4 观察项（不改代码也能活，但值得知道）

1. **seed 是 copy-once**（`profile_files.py:25` `not target.exists()`）：
   `knowledge:` 注释示例只有全新安装能看到；存量用户启用嵌入要自己去改
   `~/.agent-smith/config.yaml`，目前没有任何文档说明这一点。
2. **CLAUDE.md §9 基线漂移**：文档写 engine 980 / server 222，现为 engine
   1030（含 P1-1 失败项）/ server 231。按 CLAUDE.md 自己的规则（代码赢，
   顺手修文档），下次改动时应更新。
3. **FTS 作用域的 IN 占位符规模**：`search.py:214-221` 把全部 legacy episode id
   拼进 `IN (...)`。SQLite < 3.32 的 999 变量上限在数百 episode 时会炸；现代
   Python 自带的 SQLite（32766）没有实际风险，记录备查。
4. **嵌入层延迟上界**：启用后每轮最多 2 次 httpx 调用、各 30s 超时
   （`embeddings.py:21`），provider 挂起时一轮准备最多多等 ~60s 才降级 FTS。
   可考虑收紧到 5–10s——语义检索是锦上添花，不值得等半分钟。
5. **memory_ops 的旧包装器** `search_relevant_memories`（`store.py:53-55`）不传
   embedding provider，工具侧检索只有 FTS+路由、无语义层——行为一致性可接受，
   属已知取舍。

---

## 三、PR #57 自身质量结论

diff 走读没有发现新引入的正确性缺陷。提交说明里声称修掉的八类缺陷
（空分类清空快照、重复 topic 重复生成、`.bak` 孤儿、durable 复述剪切句子、
向量损坏永久丢失、快照两 map 不一致、未变 topic 重复生成、失败静默饿死）
**每一条都有对应测试钉住**，且测试断言的是行为而非实现细节。路由/作用域的
安全性质（未路由主题页不可达、短词 LIKE 通道不能逃逸作用域）也有独立测试。
层边界干净：engine 不知 FastAPI，`agents/` 仍零 import，server→engine 单向。

两处真正的闭环缺口都在 PR 边界之外：一处是它声称要服务的**用户可见性**没有
走完最后一跳（P1-2），一处是它写入的命名空间与既有用户数据的**共存假设**
不成立（P2-1）。

---

## 四、修复状态（本分支，紧随评审提交）

| 发现 | 状态 | 实现 |
|---|---|---|
| P1-1 git 身份 | ✅ 已修 | `_resolve_git_identity` 以只读 `git config --get`（可见宿主真实配置栈，不执行任何仓库控制代码）解析身份，经 `-c` 钉进 commit；`user.useConfigOnly=true` 彻底禁止 `user@fqdn` 杜撰。原脆弱测试钉住配置来源，另增 3 条身份契约测试（全局身份生效、repo-local 覆盖全局、无身份时确定性失败）。 |
| P1-2 shell 断链 | ✅ 已修 | `api.ts` 类型补齐并对 `last_error` 过 `sanitizeTerminalText`；HUD 新增 `memory stalled ×N` 指示（阈值 3，ERROR 色，附截断错误），失败状态优先于一切"queued"标签。3 条新测试。 |
| P2-1 级联删除用户记忆 | ✅ 已修 | association 快照记录生成页内容 hash（`hashes` map，向后兼容加载）；`_remove_topic_file` 删除前比对，内容已被覆盖的页面只解除关联、连同 `.bak` 一并保留；reuse 分支携带旧 hash 而非重算，防止用户内容被"重新认领"。4 条新测试含端到端链路。 |
| P3-1 向量缓存抖动 | ✅ 已修 | `TopicVectorIndex.sync` 改为按主题合并：未路由主题的向量保留，离开快照的主题按 `valid_topics` 修剪，结果无变化时跳过热路径写盘。3 条新测试。 |
| P3-2 topic_sync 观测盲区 | ✅ 已修 | 同步成败均写 `memory_history`（`target="topic_sync"`，失败进 streak、成功终结 streak）；`maintenance_status` 与 server schema 新增 `topic_sync: idle\|pending` 字段。端到端测试覆盖失败→pending→恢复→清零。 |
| P4-4 嵌入超时 | ✅ 已修 | httpx 超时 30s → 10s（热路径最多两次调用）。 |
| P4-1 seed copy-once 未记档 | ✅ 已修 | CLAUDE.md §7 补充说明。 |
| P4-2 基线漂移 | ✅ 已修 | CLAUDE.md §9 更新为下表数字。 |
| P3-2 的重试上限、P4-3/P4-5 | ⏸ 未动 | 冷却已防打爆；占位符规模与旧包装器为记录性观察。 |

**修复过程中新发现并已修复**：server 测试套件打印总结后**永不退出**——
`tests/test_auto_task_manual_stop.py` 每个测试开一个 `aiosqlite` `:memory:`
连接且从不关闭，而 aiosqlite 每个连接持有一个非 daemon 工作线程，
`threading._shutdown` 无限等待（py-spy 证实 4 个
`_connection_worker_thread`）。CI 上表现为测试全绿但任务挂到超时。已改为
yield-fixture 统一关闭连接；全套件现以 exit 0 结束。

修复后基线：engine **1040 passed**（59 skipped，0 failed）；server
**231 passed**（5 skipped，进程正常退出）；shell **278 passed**（11 失败仍为
CLAUDE.md §9 记录的容器 auth_token 噪音）。
