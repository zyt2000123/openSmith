# 对抗性审查 2026-08-03

分支 `audit/adversarial-2026-08-03`，基线 `701bf00`。

## 范围与方法

审查范围是 `6c06489..701bf00` 这 79 个提交引入的全部非测试源码：16 个新文件 + 10 个重度改动文件，约 5000 行。

方法为三阶段：

1. **并行 find** — 6 个独立 agent，按*攻击面*而非目录切分（按目录切会让跨文件的信任边界漏洞落在两个 agent 之间的缝里）。每个 agent 的验收标准是「具体输入 → 具体错误输出」，并被告知"3 个真 bug 胜过 15 个猜测"。
2. **独立反驳** — 3 个 agent，只拿到*结论*、不拿推理链，默认立场为「这条是错的」，不确定即判 REFUTED。
3. **分级报告** — 本文件。

基线测试：engine 1025 passed，server 222 passed / 5 skipped，两边全绿。因此以下每一条都是现有测试套件的盲区。

共 37 条发现，其中 4 条初判 P0。经反驳后：**2 条 P0 坐实，1 条降级为沙箱逃逸，1 条降级为非安全缺陷，1 条 SSRF 断言被完全驳回。**

---

## 一、必须立即修（P0，已独立复现）

### P0-1 审计日志的防篡改机制整体失效

`common/hash_chain.py:63-66, 377-385, 410-413`

删掉任意一条记录的 `"hash"` 字段，它就降级为「legacy 记录」跳过全部校验；而下一条记录存的 `prev_hash` 本来就是「排除 hash 字段算出的哈希」——**恰好等于**降级后重算的值。加上 `:395` 对首条链式记录跳过 seq 连续性检查，可任意删除日志前缀并填入伪造内容。

实测（8 条记录，删前 4 条并剥掉新首行的 hash 字段）：

```
baseline : ok=True  records=8  anchor_matches=True
攻击后   : ok=True  records=4  anchor_matches=True
幸存记录 : ['secret-command-5', ..., 'secret-command-8']
```

anchor 只钉住链*头部*的 `(seq, hash)`，不钉长度也不钉起点，所以锚点照样匹配。

**根因**：用「某字段不存在」来判定旧格式，而那个字段恰好是攻击者能删的。等价于 TLS downgrade / JWT `alg:none`。任何"缺少标记即旧格式"的向后兼容设计，在安全上下文里都是可降级攻击。

**修法**：让首条链式记录*正向声明* legacy 段的长度（代码里已有半成品 `_legacy_linked`，`:139-140`，但 `verify_chain` 从不读它），并把 `records` 计入 anchor。

**连带**（同一文件，未单独复现但机制清晰）：
- `:100, 268-305` 每条消息都新建 `ToolGuard` → 新建 `AuditLog` 实例，两个并发会话各持一份 `_next_seq`，正常并发就会把共享链写坏，且失败被 `tool_guard.py:570-571` 吞掉。
- `:142-164` 崩溃留下的半行只跳过不修复，下次 append 直接拼接上去，该行永久不可解析。
- `:194-198` 生产中 `seal()`/`close()` 从不被调用，`audit.jsonl.head` 从未写出，回滚检测从未生效。

### P0-2 grep 绕过全部敏感文件读保护

`engine/safety/tool_guard.py:377-389` + `agents/tools/grep.py:8-36`

守卫检查的是「模型声明的那个路径」，而 grep 的实际读取范围是那个路径的**整棵子树**。`:382` 是 `name = target.name.lower()` —— 对目录来说 `name` 是目录名，`_is_sensitive_read_name("myproject")` 自然返回 False。

实测（合成密钥）：

```
read_file(.ssh/id_rsa)  → allowed=False  [sens-file-002] 硬阻断
read_file(.env)         → allowed=False  [sens-file-003] 硬阻断
grep(path=<workspace>)  → allowed=True   放行

grep 实际返回：
  .npmrc:1://registry.npmjs.org/:_authToken=npm_SYNTHETIC
  certs/server.key:2:SYNTHETIC
  .env:1:STRIPE_SECRET_KEY=sk_live_SYNTHETIC...
  .ssh/id_rsa:2:SYNTHETIC
```

grep 声明 `approval_policy: "never"`，`EXCLUDED` 只有 `.git/node_modules/__pycache__/.venv/dist/.build/*.egg-info`，无任何密钥条目。这同时绕过 `_ALWAYS_BLOCKED`（`.ssh/.gnupg/.aws/.kube`）和 `agents/safety/dangerous_commands.json` 的全部规则（`sens-file-001..004` 的 `tools` 字段只列了 `shell`/`read_file`/`write_file`，没有 `grep`）。

**根因与修法**：`tool_guard.py:213-216` 的注释表明作者**已经知道**"免审批的读工具需要额外防护"，并为此专门写了 `_is_sensitive_read_name` —— 但只修了 `read_file` 一个点。修复动作应是枚举所有 `approval_policy: "never"` 的工具，逐个问「它的实际读取范围是否大于它声明的路径参数」，而不是再补一个特例。

---

## 二、已确认的功能性损坏（当前就是坏的）

### H-1 长工具轮次必定杀死 run

`engine/context/compression.py:98`

`prune_tool_outputs` 反向扫描时，只在已数到 2 条 `role:"user"` 之后才处理 `role:"tool"` —— 但工具结果**只存在于最后一条 user 消息之后**（`session_repo.add_message` 的 4 个调用点全是字面量 `"user"`/`"assistant"`，schema 的 CHECK 约束也不允许 `tool` 落库）。因此 `turns` 永远为 0，函数**永久返回 0**。

真实 `react_event_loop` 实测，8 个 `read_file` 各返回 50KB（均在截断上限内）：

```
events: ['context_compression_start', 'context_compression_end', 'context_usage', 'incomplete']
provider chat calls: 0
INCOMPLETE: reason=context_capacity_exhausted, fit_status=unfit_request, actions=[]
```

`actions=[]` 是关键——`fitting.py:138-144` 在 LLM 压缩和确定性裁剪之前就早退了。阈值约为单轮 330KB 工具输出（≈7 个满额截断的结果）。

覆盖它的两个测试（`test_audit_review_fixes.py:139-170`）构造的是 `[system, user, tool, tool, tool, user, user]` —— 引擎生产不出来的形状。

### H-2 压缩后 resume 丢掉全部近期上下文

`server/app/services/session_service.py:226`

`cutoff` 是会话级的**绝对**消息计数，却被当作**相对**索引切片到一个已被限制为 10 行的列表上：`prior_rows = prior_rows[cutoff:]`。

```
total=60 cutoff=0  -> ['msg-49' ... 'msg-58']
total=60 cutoff=10 -> []
total=60 cutoff=30 -> []
```

任何 `cutoff >= 10` 的压缩会话（即几乎所有真实压缩）在 resume 时只拿到摘要和**零条**近期消息，然后基于这个上下文重新生成回复，并覆盖掉原有的部分输出。

### H-3 压缩后历史窗口取到最老的一页尾巴

`server/app/services/session_service.py:197-198`

`get_messages(offset=cutoff)` 默认 limit=200（`session_repo.py:156`），升序返回 `[cutoff, cutoff+200)`，再取 `rows[-10:]` 得到的是 `cutoff+190..cutoff+199` —— 那一页的**最老尾巴**，不是最新消息。

```
compressed, 250 since cutoff: total=260 cutoff=10 -> msg-200..msg-209  newest=msg-259
```

前置条件较窄（`/compress` 之后又累积 >200 条且未再压缩），且当前用户消息仍单独送达引擎，所以只有 10 条历史窗口变陈旧。与 H-2 同源（绝对量当相对量用）。

### H-4 改为 manual 不停止调度，多跑一次无人值守 run

`server/app/services/auto_task_service.py:149` + `server/app/infrastructure/repositories/auto_task_repo.py:81`

服务层正确算出 `next_run_at = None`，但仓储层的 `if field in updates and updates[field] is not None` 把 `None` 过滤掉了，陈旧的 `next_run_at` 存活；`list_due_tasks` 又不按 `trigger_type` 过滤。

```
STEP 3 service returned: {'trigger_type': 'manual', 'next_run_at': '2026-08-03T06:05:12+00:00'}
STEP 5 list_due_tasks(): [('9d4e78c88489', 'manual', '...')]
still_due = True
```

用户把失控的定时任务改成 manual、API 返回 200，下一个 tick 仍会启动一次完整引擎轮次（LLM 花费 + `working_dir` 内的真实副作用），跑完才自愈。`enabled=False` 是能正常工作的对照路径。

### H-5 cost_tracker 双重失效

`agents/smith/hooks/cost_tracker.py:16` 与 `:60-62`

两个**独立**缺陷，各自都足以让它全哑：

1. `from common.paths import get_data_root` —— 该符号在仓库中不存在（全仓库仅在本文件出现两次，是对已删除函数的悬空引用）。hook 在加载期 ImportError，静默不注册，而 `hooks.yaml` 里是 `enabled: true`。
2. 即便修好导入，唯一调用方 `lifecycle.py:750` 硬编码传 `"session_stats": {}`，`if not session_stats: return` 立刻退出。

```
PRE : {'config-protection': ...}
POST: {'console-warn': ..., 'quality-gate': ...}
STOP: {}          <- cost-tracker 缺席
```

`~/.agent-smith/metrics/` 从未写出。唯一为无人值守任务设计的成本管控是死的。（DB 侧 `token_usage_events` 由 `session_service.py:638-641` 正常记录，所以"成本追踪完全失效"言过其实——死的是文件侧指标。）

**附带**：`:109-113` 未知模型回落到 Claude Sonnet 定价；`claude-3-5-haiku` 匹配不到任何键，被按 12× 真实价格计费。修好前两条后，产出的会是"自信的错数"而非空文件。

### H-6 中文路径下密钥被提交，守卫静默放行

`agents/tools/git_ops.py:303-305`

dry-run 那条命令加了 `-c core.quotePath=false`，紧接着的已暂存扫描没加。路径含任何非 ASCII 字节时 `git diff --name-only --cached` 返回 C-quote 形式，`_SENSITIVE_PATTERNS` 中所有以 `$` / `($|\.)` 收尾的分支全部失配。

```
中文目录 : "proj/\351\241\271\347\233\256/.env"  -> 提交成功，零警告
ASCII对照: proj/proj2/.env                       -> Error: refusing to stage sensitive files
```

**这个仓库本身就有中文文件名**（本次 `git diff --stat` 输出里即出现 C-quote 路径），所以不是边缘条件。攻击者不控制目录名，真实触发场景是：用户自己 `git add .`，然后让 Smith 提交——审批卡只显示 `files=[...]`，从不显示索引内容。`action: "push"` 完全没有扫描，密钥随后离开本机。

**修法**：`core.quotePath=false` 不够（含 `"` 或 `\` 的路径仍被无条件引用），应改用 `git diff --name-only -z --diff-filter=ACMR --cached` 按 `\0` 分割。同一处 `_, staged_out, _ =` 丢弃返回码，命令失败时静默产生空列表，fail-open。

覆盖它的测试（`test_tool_design_fixes.py:347-364`）monkeypatch 掉 `_run_git` 并手喂 ASCII 字符串，从未见过真实 git 的输出。

---

## 三、中优先级

| # | 位置 | 问题 |
|---|---|---|
| M-1 | `agents/smith/hooks/quality_gate.py:169-179` | 一次已批准的**写**被静默升级为无沙箱**主机执行**。`cargo clippy` 会编译并运行 `build.rs`，cwd 取自模型选定的路径，env 携带真实 `$HOME`。项目自己的 `shell` 工具走 Seatbelt 沙箱（`shell.py:46`），此处绕开。实测 `build.rs` 写出标记文件。**修法**：cwd 改为 run 绑定的工作区根，`cargo clippy` 走同一套 Seatbelt 或直接去掉。 |
| M-2 | `common/paths.py:71-78` | `_ensure_private_dir` 对已存在目录直接 return，不重新 assert `0o700`。任何在默认 umask 下由外部创建的 `~/.agent-smith`（旧版本、手动 mkdir、rsync 备份、Docker volume）永久保持 0755，SQLite 库与凭据全局可读。同文件 `_ensure_managed_directory:130-131` 是每次重申的，不一致。 |
| M-3 | `engine/safety/tool_guard.py:168-172` | `[unsafe-alias-001]` 硬链接防护只覆盖*顶层*密钥文件。`os.link(<ws>/backend/.env, notes.txt)` 后写 `notes.txt` 仅需普通审批；顶层 `.env` 的同样操作是 CRITICAL 阻断。`<service>/.env` 正是 monorepo 常态。 |
| M-4 | `engine/safety/tool_guard.py:461` | inode 检查只在 `if writing:` 分支内。经硬链接**读**取密钥完全免费，而 `_shares_inode_with_runtime_credential` 在每次读时都跑——只有用户密钥集被漏掉。 |
| M-5 | `engine/safety/tool_guard.py:233` vs `:210-228` | `.npmrc`/`.pypirc` 写需高危审批、读完全免费（`read_file` 是 `approval_policy: "never"`），token 直接进 transcript。 |
| M-6 | `engine/execution/pipeline/pipeline.py:141, 589-596` | pipeline 节点传 `provisional_lifecycle=False`，导致 `react_loop` 的废稿撤回机制永不触发，被丢弃的模型草稿被粘进已提交回复。shell 渲染 `'Let me check the config file first.  The timeout is 30 seconds.'`，而 DB 存的是 canonical 的 `'The timeout is 30 seconds.'`——消息在会话重载后会**静默变文**。命中 `code-review`/`requirements-research`/`tdd-development` 的常规"前言→工具调用→答案"形状。 |
| M-7 | `engine/execution/hooks/tool/loader.py:295-303` | 声明 `enabled: true` 的 hook 加载失败即静默丢弃，运行照常进行、零执行力。H-5 就是这条的实例。同样的代码路径管着唯一的安全 pre-hook `config-protection`——一个笔误就能让它变成 no-op 而无任何可见信号。单个 YAML 语法错误则让*所有*类型的 hook 注册数归零。 |
| M-8 | `engine/execution/hooks/tool/manager.py:34-37, 95` | 注册非原子：`hook.priority` 在存入之后才求值，一个未调 `super().__init__()` 的子类会让 loader 报「加载失败」但对象已在 `_pre_hooks` 里；而 `sorted()` 在 try 之外，于是**每次工具调用**都抛 `AttributeError` 打断 run，日志却说它没加载。 |
| M-9 | `engine/execution/hooks/tool/manager.py:99` | 工具生命周期 hook 无超时，而同包的 `extension/manager.py:173-174` 有 30s `wait_for`。安全关键的那个反而没有界。`run_stop_hooks` 挂起会卡住会话关闭。 |
| M-10 | `engine/execution/react/react_loop.py:1119-1140` | pre-hook 阻断分支不追加 `role:"tool"` 消息，而其他所有阻断分支都追加。留下悬空 `tool_call_id` → 下一次 provider 请求 400，且阻断原因从未送达模型，它无法自我纠正。 |

---

## 四、低优先级

| # | 位置 | 问题 |
|---|---|---|
| L-1 | `engine/mcp/client.py:283, 298` | per-line 30s 超时每读一行即重置，唯一兜底是 64MiB 字节预算（不约束时间）。恶意 MCP server 每 25s 吐一行合法通知即可让交互式 run 挂死数月。实测 75s 仍挂起。**但** `auto_task_service.py:268` 有 1800s wall-clock 上限，定时任务不受影响；MCP 工具是 `approval_policy: "always"`。仅可用性影响。 |
| L-2 | `engine/mcp/client.py:734` | SSE 分支依赖 `aiter_lines()`，httpx 的 `LineDecoder.buffer` 无上限；两道字节检查都在 yield *之后*才执行。server 发送数 GB 无换行数据可致 OOM。非 SSE 分支用 `aiter_bytes` 是正确有界的。 |
| L-3 | `engine/execution/hooks/tool/manager.py:98-116` | 万能 `except Exception: continue` 会把「漏写 `async`」或「返回裸 `False`」的 deny 静默转成 allow。**非安全缺陷**：hook 是三层防护最弱一层，`ToolRegistry.execute` 会独立重跑 `ToolGuard.check`；生产仅有的一个 pre-hook 形状正确；能添加 hook 的人本就有进程内代码执行权。真实受害者是手滑的 hook 作者。修法是在 `_load_pre_hook` 做 `inspect.iscoroutinefunction` 契约检查，注册期拒绝而非运行期吞掉。 |
| L-4 | `engine/execution/hooks/tool/loader.py:256-264` | 相对 `module:` 先对源码树根解析、再对配置自身目录解析，且无穿越检查。`~/.agent-smith/hooks.yaml` 里写 `module: "hooks/audit.py"` 会在当前仓库恰好存在该路径时加载**仓库里的**文件。 |
| L-5 | `engine/execution/hooks/tool/loader.py:305-314` | YAML 的 `priority` 对任何自行声明 `priority` 属性的 hook 静默失效——而唯一出厂的 pre-hook `ConfigProtectionHook:57-59` 正是如此。配置改了不生效也不告警。 |
| L-6 | `common/paths.py:28-35` | `_ensure_real_path` 拒绝*任意祖先*为符号链接，包括受管树之外的。macOS `/var`、`/tmp` 均为符号链接，`$HOME` 在外置卷或容器里指向临时目录时启动直接 `RuntimeError`。真正护住边界的是 `_ensure_real_descendant`（正确地只看根之下的部分）。 |
| L-7 | `common/hash_chain.py:151` | `O_APPEND` 分支只对自己创建的文件 chmod，已存在的 0644 链日志保持全局可读；`keep_handle` 分支则每次无条件纠正。与 M-2 叠加即得到全局可读的工具调用日志。 |
| L-8 | `common/hash_chain.py:194-198` | 对零记录链 seal 会写出 `seq: 0` 的锚点，而序号从 1 起，永远无法验证，该日志永久报篡改。 |
| L-9 | `engine/execution/pipeline/pipeline.py:449-467` | backtrack 只驱逐内存中的输出，磁盘 checkpoint 仍指向失败节点之后。该窗口内崩溃则恢复时正好跳过 backtrack 要求重跑的节点。当前无出厂 pipeline 声明 `backtrack:`，仅影响用户自建 pipeline。 |
| L-10 | `engine/execution/orchestration/run_state.py:743` | `return` 出现在它自己注释所说的「fall through」之前，已批准的工具调用被投影为**零**个事件，`event_seq` 不增，`last_event_type` 仍报审批前的事件。 |
| L-11 | `server/app/services/session_service.py:259` | `delete_session` 在 guard 之外 pop 流锁，且不像 `:54-57` 那样检查 `locked()`，直接违反 `:43-44` 注释声明的不变量。可致两个并发流在不同锁下交叉写入。 |
| L-12 | `server/app/services/auto_task_service.py:249, 262` | `load_runtime_identity_catalog()` 同步执行完整目录扫描 + `exec_module` 全部 provider，实测热态 ~25ms，`_execute` 每次付两遍。4 个并发任务启动会阻塞事件循环 ~220ms，期间所有 SSE 流冻结。 |
| L-13 | `engine/safety/tool_guard.py:813-824` | 规则文件缺失时静默产生零规则守卫（畸形文件则按设计大声报错）。`_is_agent_smith_root` 不要求该文件存在，故一个通过校验的 `AGENT_SMITH_PROJECT_ROOT` 可产出无规则守卫，20+ 条 shell 规则全部消失。 |

---

## 五、被驳回与降级的断言

诚实记录，避免下次重复调查。

| 原断言 | 裁定 | 理由 |
|---|---|---|
| `web_crawl` 渲染路径 DNS rebinding SSRF（原判 P1） | **REFUTED** | 代码描述完全准确（确无 pinning，确为二次解析），但 `playwright` 是可选 extra，两个 venv 均未安装，仓库中无任何安装入口。`_BrowserRenderer.render` 在本仓库产出的所有安装中都是死代码。*若*有人装了 `browser` extra 则成为真实（需审批的）SSRF，一行硬化：启动传 `--host-resolver-rules=MAP <host> <pinned-ip>`。 |
| hook fail-open 是 P0 安全缺陷 | **降级为 L-3** | 机制真实，安全定性错误。见 L-3。 |
| `quality_gate` 是"无审批的任意 RCE" | **降级为 M-1** | 代码执行真实，但"no approval prompt"是事实错误：三次写入每次都弹审批卡，实测即便授予并把整个目录加入会话白名单后 `approval_required` 仍为 True。正确表述是沙箱逃逸。攻击链在既有 Rust 项目中是 2 次写而非 3 次。 |
| MCP 挂起影响"任何 run" | **降级为 L-1** | `auto_task_service.py:268` 有 1800s wall-clock 上限。 |
| cost_tracker 失效的机制是空 `session_stats` | **机制修正** | 真正的首要原因是死导入，hook 根本没加载；空 dict 是独立的**第二**道缺陷。只修 dict 不会有任何变化。 |
| "成本追踪完全失效" | **范围修正** | 文件侧指标是死的，DB 侧 `token_usage_events` 正常工作。 |
| 审查期间仓库 HEAD 被并发进程移动 | **误报** | reflog 显示的移动全部来自本次会话自身的分支切换，工作区始终只有预先存在的 `AGENTS.md`。 |

---

## 六、为什么 1247 个通过的测试没抓住这些

这批缺陷里没有一条是"忘了写测试"。逐条看下来是同一个模式：**测试和代码共享了同一个错误假设**。

- **H-1** 唯一的剪枝测试构造 `[system, user, tool, tool, tool, user, user]` —— 引擎生产不出来的形状。
- **H-3** 伪造的 repo 的 `get_messages` 省略了生产里 `limit=0 → 200` 的默认值，也就是致 bug 的那一行。
- **H-4** `update_auto_task` 只测了 422 校验和跨 agent 归属，从未测过 trigger-type 转换。
- **H-5** 没有任何测试加载真实的 `agents/smith/hooks.yaml` 并断言 stop-hook 注册表非空。一行 `assert registry._stop_hooks` 就能抓住。
- **H-6** 测试 monkeypatch 掉 `_run_git` 并手喂 ASCII 字符串，从未见过真实 git 的引号输出。

推论：修这些 bug 时**必须连测试一起改**，否则测试会替 bug 辩护。

---

## 附录：复现脚本

会话 scratchpad 下（非仓库内）：

- `verify_p0.py` — P0-1 哈希链前缀删除
- `verify_grep_p0.py` — P0-2 grep 绕过
- `refute1/`、`refute2/`、`refute3/` — Phase 2 反驳探针

## 文档漂移（非缺陷）

`CLAUDE.md` §9 记的基线 "engine 717 passed, server 155 passed" 两个数都已过时，实际为 engine 1025、server 222/5 skipped。
